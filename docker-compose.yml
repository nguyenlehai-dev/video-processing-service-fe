services:
  frontend:
    build:
      context: .
      dockerfile: ${FRONTEND_DOCKERFILE:-Dockerfile}
    container_name: ${FRONTEND_CONTAINER_NAME:-video-frontend}
    ports:
      - "${FRONTEND_PORT:-8080}:80"
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
